# AI Company Core

这是跨项目、跨 Agent、跨设备的统一知识结构。

原则：
- 项目事实归 `projects/`
- Agent 运行与协作信息归 `agents/`
- 设备环境归 `devices/`
- 公司级规则归 `shared/`
- Session、state.db、API Key、Token、.env、credentials 不进入仓库

当前仓库为公开状态，因此暂时只放非敏感结构与说明。建议改为 Private 后，再同步项目细节。
